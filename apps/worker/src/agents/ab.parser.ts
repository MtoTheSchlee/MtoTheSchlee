// Kept as a thin re-export so agent code imports don't change.
// The implementation now lives in @kk/shared so the API can use it too.
export { parseAbBasic, type ParsedAb, type ParsedAbItem } from '@kk/shared';
